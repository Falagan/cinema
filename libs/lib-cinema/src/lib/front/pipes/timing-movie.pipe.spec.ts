import { TimingMoviePipe } from './timing-movie.pipe';

describe('TimingPipe', () => {
  it('create an instance', () => {
    const pipe = new TimingMoviePipe();
    expect(pipe).toBeTruthy();
  });
});
